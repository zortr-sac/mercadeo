import {
  Briefcase,
  MessageCircle,
  Package,
  Route,
  type LucideIcon,
} from "lucide-react";

export const PLAYBOOK_ICONS: Record<string, LucideIcon> = {
  Briefcase,
  MessageCircle,
  Package,
  Route,
};

export const FALLBACK_PLAYBOOK_ICON = Route;
